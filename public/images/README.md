# Portfolio image documentation

- `projects/` stores project documentation screenshots.
- `experience/` stores work and experience documentation.
- `profile/` stores the profile photo.
- `education/` stores education documentation.
- `organization/` stores organization and event documentation.

Add image paths to the corresponding data object only after the image file exists in `public/images/`.
